# 保留拓撲的整合ER

- 原始七圖全部128節點文字／形狀與128邊映到實際SVG，依有schema完整表名保守去重，121可見節點。
- 框線色表示物件類別，圖例與文字並用；未知schema、PK/FK、基數、方向不推定。
- 閱讀與題目共用同一座標圖；fit低於可讀比例改沿線逐點。關係清單逐edge配對，分支巡覽遵循真實邊。
- 保留原網站入口、七張原圖與來源詳情；來源編號不放主圖。
- 實作後由多agent獨立評分，保留低分與修正紀錄；完成技術檢查與真人驗收分開。
