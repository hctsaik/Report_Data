# MES-031 MCS Macro／Micro

使用者廠內定義：Macro command描述E2E；Micro command細至每個軌道轉彎處。依傳送命令可追FOUP在何時何處、往何處移動。
僅更新MCS兩節點與MCS閱讀路徑；MES傳送節點不任意套入MCS層級定義。
補比較、查詢順序、同FOUP時間位置互動軌道路線。採精確HTML/SVG示例，不推定兩個歷史表分別等於macro或micro，不新增SQL欄位。
示例Macro A→D、Micro A→B→C→D，B/C為轉彎點；四時間是假設事件，不把命令目的地當成已到站。
測試節點/路徑、桌面手機逐時點選、展開、導航與主題切換。
